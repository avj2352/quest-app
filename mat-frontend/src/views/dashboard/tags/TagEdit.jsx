import React from 'react';
import Typography from '@material-ui/core/Typography';

const TagEdit = props => {
    return (
        <Typography component="p">TagEdit Page</Typography>
    );
}

export default TagEdit;