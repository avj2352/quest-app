import React from 'react';
import Typography from '@material-ui/core/Typography';

const GroupEdit = props => {
    return (
        <Typography component="p">GroupEdit Page</Typography>
    );
}

export default GroupEdit;