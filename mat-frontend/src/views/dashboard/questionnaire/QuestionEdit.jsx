import React from 'react';
import Typography from '@material-ui/core/Typography';

const QuestionEdit = props => {
    return (        
        <Typography component="p">QuestionEdit Page</Typography>
    );
}

export default QuestionEdit;